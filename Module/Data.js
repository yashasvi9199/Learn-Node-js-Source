//Secret names
const secretName = "TOP SECRET"

// Sharing names
const name1 = 'john'
const name2 = 'peter'

module.exports = {name1, name2}

// something to observe is that module > exports returns an Object
// Which is why we are returning it in a way like this as mentioned above